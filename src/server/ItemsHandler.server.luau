-- Script in ServerScriptService named "ItemsHandler"
-- Item inventory rewards are disabled for beta. This script intentionally does
-- not create item inventories, starter bundles, boost folders, or item grants.
warn("ItemsHandler: item inventory runtime grants disabled for beta.")
